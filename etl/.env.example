# Copy to .env (600 perms), fill in the etl_pw set in db/roles.sql.
DATABASE_URL_ETL=postgresql://excise_etl:CHANGE_ME_etl@127.0.0.1:5432/excise_bank

# Root of the NITI submission workbooks (DATA_PIPELINE.md §Source adapters).
NITI_SOURCE_DIR=/home/subhan/mentor_portal_db/UP Excise Data Collection

# pdf-markdown-pipeline sync (DATA_PIPELINE.md §pdf-markdown-pipeline sync).
# excise_mcp_kb_ro is created by OPERATOR_SETUP.md §Data bank, SELECT-only on
# pdf_markdown_pipeline_local — fill in the password set there.
KB_RO_MYSQL_HOST=127.0.0.1
KB_RO_MYSQL_PORT=3306
KB_RO_MYSQL_USER=excise_mcp_kb_ro
KB_RO_MYSQL_PASSWORD=CHANGE_ME_kb_ro
KB_RO_MYSQL_DATABASE=pdf_markdown_pipeline_local
